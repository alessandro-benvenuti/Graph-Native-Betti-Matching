defaults:
  - smoke_mixed.yaml
  - losses/focal.yaml
  - losses/betti.yaml

experiment:
  name: smoke_mixed_focal_betti

topology:
  betti_h0:
    warmup_epochs: 0
    ramp_epochs: 0
  betti_h1:
    warmup_epochs: 0
    ramp_epochs: 0
