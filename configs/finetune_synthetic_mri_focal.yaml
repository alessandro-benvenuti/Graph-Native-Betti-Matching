defaults:
  - finetune_synthetic_mri.yaml

experiment:
  name: finetune_synthetic_mri_focal

loss:
  edge:
    classification:
      name: focal
      class_weights: [1.0, 1.0]
      focal_gamma: 2.0
    candidates:
      max_per_graph: 9999
      positive_cap: null
    balancing:
      mode: none
