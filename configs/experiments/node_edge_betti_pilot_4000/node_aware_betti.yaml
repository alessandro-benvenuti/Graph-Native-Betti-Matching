defaults:
  - common.yaml
  - ../../losses/betti_node_edge.yaml

experiment:
  name: node_edge_betti_pilot4000_nodeaware_e100_seed364505

topology:
  betti_h0:
    weight: 0.01
    warmup_epochs: 5
    ramp_epochs: 10
  betti_h1:
    weight: 0.01
    warmup_epochs: 5
    ramp_epochs: 10
