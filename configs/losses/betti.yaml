topology:
  betti_h0:
    enabled: true
    log_only: false
    weight: 0.1
    warmup_epochs: 10
    ramp_epochs: 20
  betti_h1:
    enabled: true
    log_only: false
    weight: 0.1
    warmup_epochs: 10
    ramp_epochs: 20
